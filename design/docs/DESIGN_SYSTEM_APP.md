# Design system (in-app)

Open **`/design`**.

- Live demos for foundations + components (logo, color, type, layout, motion, images, buttons, cards, …)
- Version switcher, **Live** badge, **Make live**, and **New version** live in the docs header (no separate versions URL)
- Optional DB: `DESIGN_SYSTEM_DATABASE_URL` (falls back to `DATABASE_URL`)
